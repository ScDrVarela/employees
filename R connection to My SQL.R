#Te first lines will stablish the connection with MySQL Database

mysqlconnection = dbConnect(RMySQL::MySQL(),
                            dbname = 'ferresteel',
                            host = 'localhost',
                            port = 3306,
                            user = 'root',
                            password = 'Ferrestill2712')
